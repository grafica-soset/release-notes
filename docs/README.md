# Documentação

- [**Integração com a API**](./integracao-api.md) — como um sistema externo lista
  as últimas releases e abre bug reports no release-notes (endpoints `/api/public/*`,
  autenticação por `x-api-key`, exemplos em cURL e JavaScript).
- [**Deploy no Google Cloud Run**](./deploy-cloud-run.md) — publicar a app usando
  o `Dockerfile` e o `cloudbuild.yaml` (Artifact Registry, Secret Manager, triggers).
