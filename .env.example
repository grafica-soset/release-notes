# Copie este arquivo para `.env` e ajuste a string de conexão.
MONGODB_URI=mongodb://localhost:27017/release-notes

# Chave da API de integração (/api/public/*). Se definida, os endpoints
# exigem o header `x-api-key: <esta-chave>`. Se ausente, ficam abertos.
INTEGRATION_API_KEY=d1703583-9144-403b-9263-5f2088228472
